use crate::resource::handle::ResourceHandle;

use super::context::Context;

pub struct Activity;

impl Activity {
    pub fn start(ctx: &mut Context, bootstrap: ResourceHandle) -> ActivityId {
        todo!()
    }
}
